javascript:(() => { let lastClickedButton = null; const clickButton = () => { const button = [...document.querySelectorAll('.live-streams-monitoring-point_popular__x1FQU')].find(b => b !== lastClickedButton); if (button) { button.click(); lastClickedButton = button; } }; clickButton(); new MutationObserver(() => { const newButton = [...document.querySelectorAll('.live-streams-monitoring-point_popular__x1FQU')].find(b => b !== lastClickedButton); if (newButton) clickButton(); }).observe(document.body, { childList: true, subtree: true }); })();
